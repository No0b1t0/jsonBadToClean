{
  "name": "loader_service",
  "version": "1.0.0",
  "description": "service that loads garbage json file cleans it, manipulates it, sends it to the relevant queue",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Arik Gortsunian",
  "license": "ISC",
  "dependencies": {}
}
